"""Host-runnable pure logic for AgriControl: canonical state, decision engine, and safety supervisor.

This package has no GPIO, PWM, Wi-Fi, HTTP, or browser dependencies so it can
run on a computer today and be ported into ESP32 MicroPython firmware later
without rewriting the rules themselves.
"""
